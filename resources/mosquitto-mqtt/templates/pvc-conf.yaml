kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mosquitto-conf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  volumeName: {{ .Values.existingVolume.conf }}