apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ring-mqtt.fullname" . }}-config
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi